/*** HELP START ***/

/* >>> %loopEnd() macro: <<<
 * 
 * The macro ends looping over all elements of an array.
 *
 * Example 1:
    %loopOver(ABC); 
      put _all_;; 
    %loopEnd;           
 *
**/
/* The definition: */
%macro loopEnd;  /* no parameters */
/*** HELP END ***/
end;
%mend loopEnd;
